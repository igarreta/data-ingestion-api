# PostgreSQL connection
DB_HOST=10.0.100.x
DB_PORT=5432
DB_NAME=homelab
DB_USER=ingestion_api
DB_PASSWORD=changeme

# API tokens — one per client application
# Format: APP_TOKEN_<NAME>=<token>
APP_TOKEN_HOMEASSISTANT=changeme

# Pushover notifications (500 errors only)
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=
PUSHOVER_DEVICE=iphoneRSI

# Service identity (used in Pushover messages)
HOSTNAME=cygnus
SERVICE_NAME=data-ingestion-api
